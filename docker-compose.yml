version: '3'
services:
  app:
    image: antoniowebacademy/web_app:latest
    container_name: webacademydemo
    restart: always
    ports:
      - "80:3000"
    environment:
      NODE_ENV: production